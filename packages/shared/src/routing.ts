import { serviceCapacity, travelMinutes, type Assessment, type Facility, type Service, type Urgency } from "./index.ts";

export type CareLevel = "PRIMARY" | "SECONDARY" | "TERTIARY" | "EMERGENCY";
export type ServicePlan = { requiredService: Service; acceptableAlternatives: Service[]; requiredCareLevel: CareLevel; reason: string };
export type RecommendationStatus = "RECOMMENDED" | "ALTERNATIVE" | "UNAVAILABLE_BUT_RELEVANT" | "NOT_SUITABLE";
export type RouteCandidate = Facility & { facilityId:string; facilityName:string; facilityType:string; locality:string; availability:"AVAILABLE"|"LIMITED"|"UNAVAILABLE"; availabilitySource:"SIMULATED_FOR_PROTOTYPE"; serviceCapacity:ReturnType<typeof serviceCapacity>; requiredServiceMatch:boolean; careLevelMatch:boolean; score:number; travelMinutes:number; rankingReasons:string[]; recommendationStatus:RecommendationStatus; rerouteReason?:string };
export type RouteDecision = { requestId:string; plan:ServicePlan; candidates:RouteCandidate[]; selectedFacilityId:string|null; originalFacilityId:string|null; rerouted:boolean; rerouteReason:string|null; explanation:string; audit:{ urgency:Urgency; candidateCount:number; selectedFacilityId:string|null; rerouted:boolean; matchingVersion:string; timestamp:string } };
export const ROUTING_WEIGHTS={ SERVICE_MATCH:55, CARE_LEVEL:30, AVAILABLE:20, LIMITED:8, INFERRED_CAPABILITY:-4, DISTANCE_PER_KM:1.2 } as const;
const levels:Record<Facility["type"],CareLevel[]>={AAM:["PRIMARY"],DISPENSARY:["PRIMARY"],PHC:["PRIMARY"],CHC:["PRIMARY","SECONDARY"],DISTRICT_HOSPITAL:["SECONDARY","EMERGENCY"]};
const rank:Record<CareLevel,number>={PRIMARY:1,SECONDARY:2,TERTIARY:3,EMERGENCY:4};
export function classifyService(a:Assessment):ServicePlan { if(a.urgency==="EMERGENCY") return {requiredService:"EMERGENCY",acceptableAlternatives:[],requiredCareLevel:"EMERGENCY",reason:"Emergency-capable public care is required because a deterministic danger-sign rule was triggered."}; if(a.service==="CHILD_HEALTH")return {requiredService:"CHILD_HEALTH",acceptableAlternatives:["PRIMARY_CARE"],requiredCareLevel:"PRIMARY",reason:"Child-health assessment is appropriate based on the structured need."}; if(a.service==="MATERNITY")return {requiredService:"MATERNITY",acceptableAlternatives:[],requiredCareLevel:"PRIMARY",reason:"Maternal-care assessment is appropriate based on the structured need."};return {requiredService:"PRIMARY_CARE",acceptableAlternatives:[],requiredCareLevel:"PRIMARY",reason:"Primary-care assessment is appropriate based on the structured need."}; }
function hasLevel(f:Facility,needed:CareLevel){return needed==="EMERGENCY"?levels[f.type].includes("EMERGENCY"):levels[f.type].some(level=>rank[level]>=rank[needed]);}
export function routeFacilities(facilities:Facility[],assessment:Assessment,requestId="demo-request"):RouteDecision { const plan=classifyService(assessment); const candidates=facilities.filter(f=>Number.isFinite(f.latitude)&&Number.isFinite(f.longitude)&&f.latitude!==0&&f.longitude!==0).filter(f=>[plan.requiredService,...plan.acceptableAlternatives].some(s=>f.services.includes(s))&&hasLevel(f,plan.requiredCareLevel)).map(f=>{const capacity=serviceCapacity(f,plan.requiredService),exact=f.services.includes(plan.requiredService),source=f.serviceSources?.[plan.requiredService]||f.capabilitySource||"INFERRED_FROM_FACILITY_TYPE",inferred=source==="INFERRED_FROM_FACILITY_TYPE";const score=ROUTING_WEIGHTS.SERVICE_MATCH+(exact?8:0)+ROUTING_WEIGHTS.CARE_LEVEL+(capacity.status==="AVAILABLE"?ROUTING_WEIGHTS.AVAILABLE:capacity.status==="LIMITED"?ROUTING_WEIGHTS.LIMITED:0)+(inferred?ROUTING_WEIGHTS.INFERRED_CAPABILITY:0)-f.distanceKm*ROUTING_WEIGHTS.DISTANCE_PER_KM;return {...f,facilityId:f.id,facilityName:f.name,facilityType:f.type,locality:f.address,availability:capacity.status,availabilitySource:"SIMULATED_FOR_PROTOTYPE" as const,serviceCapacity:capacity,requiredServiceMatch:exact,careLevelMatch:true,score,travelMinutes:travelMinutes(f),rankingReasons:[`${exact?"Supports required service":"Supports acceptable primary-care fallback"}: ${plan.requiredService.replaceAll("_"," ")}`,`Appropriate ${plan.requiredCareLevel.toLowerCase()} care level`,`${f.distanceKm} km from the labelled demo origin`,`${capacity.status.toLowerCase()} availability — simulated for prototype`,`${source} capability`],recommendationStatus:capacity.status==="UNAVAILABLE"?"UNAVAILABLE_BUT_RELEVANT" as const:"ALTERNATIVE" as const};}).sort((a,b)=>b.score-a.score); const original=[...candidates].sort((a,b)=>a.distanceKm-b.distanceKm)[0]||null; const selected=candidates.find(f=>f.availability!=="UNAVAILABLE")||null; const rerouted=Boolean(original&&selected&&original.id!==selected.id); const final=candidates.map(item=>({...item,recommendationStatus:(selected?.id===item.id?"RECOMMENDED":item.availability==="UNAVAILABLE"?"UNAVAILABLE_BUT_RELEVANT":"ALTERNATIVE") as RecommendationStatus,rerouteReason:item.id===original?.id&&rerouted?"REQUIRED_SERVICE_UNAVAILABLE":undefined})); const explanation=!selected?"No suitable available facility was found in the current dataset. Availability is simulated; seek human assistance for the broader public-care pathway.":rerouted?`${original?.name} is closer and relevant, but required-service availability is marked unavailable in this prototype. Rerouted to ${selected.name}, where the required service is available.`:`${selected.name} is recommended because it supports the required service, meets the care-level requirement, is reachable from the demo origin, and is available in the prototype dataset.`;return {requestId,plan,candidates:final,selectedFacilityId:selected?.id||null,originalFacilityId:original?.id||null,rerouted,rerouteReason:rerouted?"REQUIRED_SERVICE_UNAVAILABLE":null,explanation,audit:{urgency:assessment.urgency,candidateCount:final.length,selectedFacilityId:selected?.id||null,rerouted,matchingVersion:"routing-v2",timestamp:new Date().toISOString()}}; }
